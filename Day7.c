#include <stdio.h>

int main()
{
    int radius,height;
    float Area,Volume,pi = 3.14;
    printf("give the radius, height of cylinder\n");
    scanf("%d %d", &radius, &height);
    printf("Area of Circle is %f\n",Area=pi*radius*radius);
    printf("Volume of cylinder is %f\n",Volume=pi*radius*radius*height);
    return 0;
}
